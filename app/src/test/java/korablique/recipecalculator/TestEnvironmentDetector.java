package korablique.recipecalculator;

/**
 * Класс, заменяющий точно такой же не-тестовый класс, чтобы код мог понять, что он в тестах.
 */
public class TestEnvironmentDetector {
    public static boolean isInTests() {
        return true;
    }
}
