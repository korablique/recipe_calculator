package korablique.recipecalculator.base;

/**
 * Function that accepts no arguments and produces a result
 * @param <T> type of returning object
 */
public interface Function0arg<T> {
    T call();
}
